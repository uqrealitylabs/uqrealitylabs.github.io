Routes
- `/`: React shell and legacy Three scene.
- About/social/committee/rubrics are in-scene sections driven by JSON content.
- Rubrics content exists as JSON for routing/extraction and current external CTA use.

Filesystem
- index.html: React root plus generated default SEO head block.
- public/: static assets plus generated robots.txt, sitemap.xml, and llms.txt.
- src/app/: React boot shell.
- src/content/generated/content.schema.json: editor schema.
- src/content/pages/<locale>/*.json: page content.
- src/content/site/<locale>.json: socials, committee, animation copy, SEO site config.
- src/content/schema/contentSchema.ts: TS validation/types.
- src/content/contentRegistry.ts: central content imports and locale fallback.
- src/content/ContentRenderer.tsx: safe structured block renderer.
- src/seo/: pure robots/sitemap/metadata/JSON-LD builders.
- src/features/legacy-three/: lazy Three scene and DOM scaffold.
- src/shared/i18n/: shell/global UI strings.
- src/shared/state/: coarse Zustand UI state.
- src/shared/styles/: Tailwind v4 entry, tokens, base CSS, legacy CSS.
- scripts/: TypeScript-only validation/asset/SEO scripts.
- tests/unit/: Vitest data/logic tests.
- tests/e2e and tests/component: Cypress specs.

Data flow
- App sets locale metadata and lazy-loads legacy-main.
- legacy-main reads `document.documentElement.lang`, then gets pages/site data from contentRegistry.
- contentRegistry falls unknown locales back to English and throws for unknown pages.
- ContentRenderer renders validated structured sections without raw HTML.
- SEO flow: JSON site/page content -> src/seo builders -> index.html head block and public crawler files.

Checks
- `npm run test:ci` runs no-JS, content validation, SEO validation, typecheck, lint, tests, build, and budget checks.
- `npm run seo:check` fails when generated SEO files or the head block are stale.
- `npm run check:budgets` reads `dist` and fails on shell/lazy chunk gzip budget violations.

robotic-bees integration
- Source branch audited: seo-robots-content-signals.
- Ported: content signals, robots, sitemap, llms.txt, SVG favicon, metadata/social/JSON-LD builders.
- Rejected: old app/main.js edits, static hand-authored robots/sitemap, .mjs tests, Vite script changes, Safari mask icon churn, and binary icon churn.
- Current sitemap indexes `/` only; add in-scene routes after real pathname routing exists.
